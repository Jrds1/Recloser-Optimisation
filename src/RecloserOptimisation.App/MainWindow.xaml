<Window x:Class="RecloserOptimisation.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Recloser Optimisation"
        Width="1500"
        Height="900"
        MinWidth="1180"
        MinHeight="720"
        WindowStartupLocation="CenterScreen"
        Icon="Assets/icon.ico"
        Background="#F7F9FC">
    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="38" />
            <Setter Property="Margin" Value="0,3,0,3" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Foreground" Value="#DBE9FB" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#1559C9" />
            <Setter Property="BorderBrush" Value="#1559C9" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Foreground" Value="#39475D" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#DCE3EE" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#DCE3EE" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="0,0,14,14" />
        </Style>
        <Style x:Key="MutedTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#667287" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
        <Style x:Key="PageHeadingStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#132033" />
            <Setter Property="FontSize" Value="22" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#07172C">
            <DockPanel LastChildFill="True" Margin="14">
                <StackPanel DockPanel.Dock="Top">
                    <Border BorderBrush="#203852" BorderThickness="0,0,0,1" Padding="2,0,0,14" Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="Assets/icon.ico" Width="38" Height="38" />
                            <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="RecloseOpt" Foreground="White" FontWeight="Bold" />
                                <TextBlock Text="Windows app" Foreground="#A9BBD2" FontSize="12" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Button x:Name="NavDashboard" Content="Dashboard" Tag="Dashboard" Style="{StaticResource NavButtonStyle}" />
                    <Button x:Name="NavIngestion" Content="Ingestion" Tag="Ingestion" Style="{StaticResource NavButtonStyle}" />
                    <Button x:Name="NavProcessing" Content="Processing" Tag="Processing" Style="{StaticResource NavButtonStyle}" />
                    <Button x:Name="NavModel" Content="Model Building" Tag="Model" Style="{StaticResource NavButtonStyle}" />
                    <Button x:Name="NavMap" Content="Topology Map" Tag="Map" Style="{StaticResource NavButtonStyle}" />
                    <Button x:Name="NavExplorer" Content="Model Explorer" Tag="Explorer" Style="{StaticResource NavButtonStyle}" />
                    <Button x:Name="NavValidation" Content="Validation" Tag="Validation" Style="{StaticResource NavButtonStyle}" />
                    <Button x:Name="NavAnalysis" Content="Analysis" Tag="Analysis" Style="{StaticResource NavButtonStyle}" />
                    <Button x:Name="NavOutages" Content="Outages" Tag="Outages" Style="{StaticResource NavButtonStyle}" />
                    <Button x:Name="NavReports" Content="Reports" Tag="Reports" Style="{StaticResource NavButtonStyle}" />
                </StackPanel>

                <StackPanel DockPanel.Dock="Bottom">
                    <Button x:Name="NavSettings" Content="Settings" Tag="Settings" Style="{StaticResource NavButtonStyle}" />
                    <Border BorderBrush="#203852" BorderThickness="0,1,0,0" Padding="0,14,0,0" Margin="0,10,0,0">
                        <StackPanel>
                            <TextBlock Text="Project" Foreground="#A9BBD2" FontSize="12" />
                            <TextBlock Text="King Country feeder study" Foreground="White" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,3,0,10" />
                            <TextBlock Text="Jack Satherley" Foreground="White" FontWeight="SemiBold" />
                            <TextBlock Text="Research prototype" Foreground="#A9BBD2" FontSize="12" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="72" />
                <RowDefinition Height="*" />
                <RowDefinition Height="34" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#DCE3EE" BorderThickness="0,0,0,1">
                <Grid Margin="22,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="ViewSubtitle" Text="Prototype" Foreground="#667287" FontSize="12" FontWeight="SemiBold" />
                        <TextBlock x:Name="ViewTitle" Text="Dashboard" Foreground="#132033" FontSize="24" FontWeight="SemiBold" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="TopImportButton" Content="Import" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" />
                        <Button x:Name="ExportManifestButton" Content="Export State" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0" />
                        <Button x:Name="ExportTemplateButton" Content="Export Template" Style="{StaticResource SecondaryButtonStyle}" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <ScrollViewer x:Name="DashboardPage" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="22">
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Network overview" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" />
                                <TextBlock Text="No feeder model loaded" Style="{StaticResource PageHeadingStyle}" />
                            </StackPanel>
                            <ComboBox Grid.Column="1" Width="190" Height="34" SelectedIndex="0">
                                <ComboBoxItem Content="All feeders" />
                                <ComboBoxItem Content="Active workspace" />
                                <ComboBoxItem Content="Imported only" />
                            </ComboBox>
                        </Grid>

                        <UniformGrid Columns="4" Margin="0,0,0,14">
                            <Border Style="{StaticResource PanelStyle}">
                                <StackPanel>
                                    <TextBlock Text="Feeders" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" />
                                    <TextBlock Text="0" FontSize="32" FontWeight="SemiBold" Foreground="#132033" />
                                    <TextBlock Text="Awaiting GIS, DXL, DXF or CSV import" Style="{StaticResource MutedTextStyle}" />
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource PanelStyle}">
                                <StackPanel>
                                    <TextBlock Text="Protection devices" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" />
                                    <TextBlock Text="0" FontSize="32" FontWeight="SemiBold" Foreground="#132033" />
                                    <TextBlock Text="Reclosers, fuses and breakers will appear here" Style="{StaticResource MutedTextStyle}" />
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource PanelStyle}">
                                <StackPanel>
                                    <TextBlock Text="Customers represented" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" />
                                    <TextBlock Text="0" FontSize="32" FontWeight="SemiBold" Foreground="#132033" />
                                    <TextBlock Text="Customer groupings not imported" Style="{StaticResource MutedTextStyle}" />
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource PanelStyle}">
                                <StackPanel>
                                    <TextBlock Text="Validation issues" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" />
                                    <TextBlock Text="0" FontSize="32" FontWeight="SemiBold" Foreground="#132033" />
                                    <TextBlock Text="No model has been validated yet" Style="{StaticResource MutedTextStyle}" />
                                </StackPanel>
                            </Border>
                        </UniformGrid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Style="{StaticResource PanelStyle}">
                                <StackPanel>
                                    <TextBlock Text="Workflow Status" FontSize="16" FontWeight="SemiBold" Foreground="#132033" />
                                    <Border Background="#F7F9FC" BorderBrush="#C8D3E3" BorderThickness="1" CornerRadius="8" Padding="22" Margin="0,14,0,0">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock Text="Ready for source data" FontWeight="SemiBold" Foreground="#132033" TextAlignment="Center" />
                                            <TextBlock Text="Import utility exports first. Processing, validation and optimisation are reserved for the next build." Style="{StaticResource MutedTextStyle}" TextAlignment="Center" Margin="0,6,0,0" />
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>
                            <Border Grid.Column="1" Style="{StaticResource PanelStyle}">
                                <StackPanel>
                                    <TextBlock Text="Reliability Indices" FontSize="16" FontWeight="SemiBold" Foreground="#132033" />
                                    <UniformGrid Columns="3" Margin="0,14,0,0">
                                        <Border Background="#F7F9FC" BorderBrush="#EDF1F7" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,8,0">
                                            <StackPanel>
                                                <TextBlock Text="SAIDI" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" />
                                                <TextBlock Text="-" FontSize="26" FontWeight="SemiBold" />
                                            </StackPanel>
                                        </Border>
                                        <Border Background="#F7F9FC" BorderBrush="#EDF1F7" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,8,0">
                                            <StackPanel>
                                                <TextBlock Text="SAIFI" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" />
                                                <TextBlock Text="-" FontSize="26" FontWeight="SemiBold" />
                                            </StackPanel>
                                        </Border>
                                        <Border Background="#F7F9FC" BorderBrush="#EDF1F7" BorderThickness="1" CornerRadius="8" Padding="14">
                                            <StackPanel>
                                                <TextBlock Text="Baseline" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" />
                                                <TextBlock Text="-" FontSize="26" FontWeight="SemiBold" />
                                            </StackPanel>
                                        </Border>
                                    </UniformGrid>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Border Style="{StaticResource PanelStyle}">
                            <StackPanel>
                                <TextBlock Text="Recent Imports" FontSize="16" FontWeight="SemiBold" Foreground="#132033" Margin="0,0,0,12" />
                                <DataGrid x:Name="DashboardImportsGrid" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" IsReadOnly="True" MinHeight="160">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="File" Binding="{Binding File}" Width="*" />
                                        <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="160" />
                                        <DataGridTextColumn Header="Format" Binding="{Binding Format}" Width="130" />
                                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="IngestionPage" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                    <StackPanel Margin="22">
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Source staging" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" />
                                <TextBlock Text="Import utility, electrical modelling and GIS exports" Style="{StaticResource PageHeadingStyle}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button x:Name="ChooseFilesButton" Content="Import Files" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0" />
                                <Button x:Name="ChooseFolderButton" Content="Import Folder" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" />
                                <Button x:Name="ClearImportsButton" Content="Clear Staged" Style="{StaticResource SecondaryButtonStyle}" />
                            </StackPanel>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.15*" />
                                <ColumnDefinition Width="0.85*" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Style="{StaticResource PanelStyle}">
                                <StackPanel>
                                    <TextBlock Text="Import source" FontSize="16" FontWeight="SemiBold" Foreground="#132033" />
                                    <Grid Margin="0,12,0,14">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="180" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Source group" VerticalAlignment="Center" Foreground="#667287" FontWeight="SemiBold" />
                                        <ComboBox x:Name="SourceTypeCombo" Grid.Column="1" Height="34" SelectedIndex="0">
                                            <ComboBoxItem Content="GIS / mapping" />
                                            <ComboBoxItem Content="Electrical model" />
                                            <ComboBoxItem Content="Protection settings" />
                                            <ComboBoxItem Content="SCADA / ADMS" />
                                            <ComboBoxItem Content="OMS / outage history" />
                                            <ComboBoxItem Content="Customer / load" />
                                            <ComboBoxItem Content="Asset registry" />
                                        </ComboBox>
                                    </Grid>
                                    <Border Background="#F7F9FC" BorderBrush="#B7C5D9" BorderThickness="1" CornerRadius="8" Padding="24" MinHeight="158">
                                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock Text="Stage imports without processing" FontWeight="SemiBold" Foreground="#132033" TextAlignment="Center" />
                                            <TextBlock Text="Files and folders are recorded with source metadata only. Parsers, normalisation and optimisation are intentionally empty for this prototype." Style="{StaticResource MutedTextStyle}" TextAlignment="Center" Margin="0,8,0,0" />
                                        </StackPanel>
                                    </Border>
                                    <TextBlock Text="Supported source patterns include GIS, CAD, engineering models, protection settings, SCADA, OMS, AMI, asset registry and customer/load exports." Style="{StaticResource MutedTextStyle}" Margin="0,12,0,0" />
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Style="{StaticResource PanelStyle}">
                                <StackPanel>
                                    <TextBlock Text="Import adapter plan" FontSize="16" FontWeight="SemiBold" Foreground="#132033" Margin="0,0,0,8" />
                                    <TextBlock Text="The app is set up to accept common electrical engineering, GIS, protection, SCADA, outage and asset-management exports." Style="{StaticResource MutedTextStyle}" Margin="0,0,0,12" />
                                    <DataGrid x:Name="AdapterSummaryGrid" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" IsReadOnly="True" MinHeight="260">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Platform" Binding="{Binding Platform}" Width="*" />
                                            <DataGridTextColumn Header="Formats" Binding="{Binding Formats}" Width="1.15*" />
                                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="95" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Border Style="{StaticResource PanelStyle}">
                            <StackPanel>
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Staged imports" FontSize="16" FontWeight="SemiBold" Foreground="#132033" />
                                    <Button x:Name="ExportImportManifestButton" Grid.Column="1" Content="Export Manifest" Style="{StaticResource SecondaryButtonStyle}" />
                                </Grid>
                                <DataGrid x:Name="ImportsGrid" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" IsReadOnly="True" MinHeight="230">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="File or folder" Binding="{Binding File}" Width="*" />
                                        <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="170" />
                                        <DataGridTextColumn Header="Format" Binding="{Binding Format}" Width="150" />
                                        <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="110" />
                                        <DataGridTextColumn Header="Imported" Binding="{Binding Imported}" Width="150" />
                                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="ProcessingPage" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                    <StackPanel Margin="22">
                        <TextBlock Text="Pipeline" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" />
                        <TextBlock Text="Processing queue" Style="{StaticResource PageHeadingStyle}" Margin="0,0,0,16" />
                        <UniformGrid Columns="4">
                            <Border Style="{StaticResource PanelStyle}"><StackPanel><TextBlock Text="1. Identify datasets" FontWeight="SemiBold" /><TextBlock Text="Source detection and coordinate system matching will be connected later." Style="{StaticResource MutedTextStyle}" Margin="0,8,0,0" /></StackPanel></Border>
                            <Border Style="{StaticResource PanelStyle}"><StackPanel><TextBlock Text="2. Attribute mapping" FontWeight="SemiBold" /><TextBlock Text="Field names, units and IDs will map into the internal schema." Style="{StaticResource MutedTextStyle}" Margin="0,8,0,0" /></StackPanel></Border>
                            <Border Style="{StaticResource PanelStyle}"><StackPanel><TextBlock Text="3. Network graph build" FontWeight="SemiBold" /><TextBlock Text="Nodes, spans, devices and customer groupings will form a directed model." Style="{StaticResource MutedTextStyle}" Margin="0,8,0,0" /></StackPanel></Border>
                            <Border Style="{StaticResource PanelStyle}"><StackPanel><TextBlock Text="4. Validation handoff" FontWeight="SemiBold" /><TextBlock Text="Connectivity, missing attributes and feasibility checks will publish issues." Style="{StaticResource MutedTextStyle}" Margin="0,8,0,0" /></StackPanel></Border>
                        </UniformGrid>
                        <Border Style="{StaticResource PanelStyle}">
                            <TextBlock Text="Processing is intentionally not implemented yet. The desktop app exposes the queue, states and import metadata so parser services can be attached later." TextWrapping="Wrap" Foreground="#667287" />
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="ModelPage" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                    <StackPanel Margin="22">
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Internal network model" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" />
                                <TextBlock Text="Model building" Style="{StaticResource PageHeadingStyle}" />
                            </StackPanel>
                            <Button x:Name="ExportGeoJsonButton" Grid.Column="1" Content="Export Empty GeoJSON" Style="{StaticResource SecondaryButtonStyle}" />
                        </Grid>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Style="{StaticResource PanelStyle}">
                                <StackPanel>
                                    <TextBlock Text="Model versions" FontSize="16" FontWeight="SemiBold" Foreground="#132033" />
                                    <TextBlock Text="No model versions. Imported data will eventually produce versioned feeder snapshots." Style="{StaticResource MutedTextStyle}" Margin="0,12,0,0" />
                                </StackPanel>
                            </Border>
                            <Border Grid.Column="1" Style="{StaticResource PanelStyle}">
                                <StackPanel>
                                    <TextBlock Text="Target schema" FontSize="16" FontWeight="SemiBold" Foreground="#132033" />
                                    <TextBlock Text="nodes, line_segments, protection_devices, customers, outage_events, validation_issues" Style="{StaticResource MutedTextStyle}" Margin="0,12,0,0" />
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>

                <Grid x:Name="MapPage" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="292" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="300" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="#FFFFFF" BorderBrush="#DCE3EE" BorderThickness="0,0,1,0" Padding="16">
                        <StackPanel>
                            <TextBlock Text="Explorer" FontSize="16" FontWeight="SemiBold" Foreground="#132033" />
                            <TextBox Height="34" Margin="0,12,0,14" Text="Search assets..." Foreground="#667287" />
                            <Button Content="Feeders (0)" Style="{StaticResource SecondaryButtonStyle}" HorizontalContentAlignment="Left" Margin="0,0,0,6" />
                            <Button Content="Protection devices (0)" Style="{StaticResource SecondaryButtonStyle}" HorizontalContentAlignment="Left" Margin="0,0,0,6" />
                            <Button Content="Line sections (0)" Style="{StaticResource SecondaryButtonStyle}" HorizontalContentAlignment="Left" Margin="0,0,0,6" />
                            <Button Content="Customer groups (0)" Style="{StaticResource SecondaryButtonStyle}" HorizontalContentAlignment="Left" />
                        </StackPanel>
                    </Border>

                    <Grid x:Name="MapHost" Grid.Column="1" Background="#DDE7EF" ClipToBounds="True">
                        <Canvas x:Name="TileCanvas" Background="#DDE7EF" />
                        <Border HorizontalAlignment="Left" VerticalAlignment="Top" Margin="14" Background="#F9FFFFFF" BorderBrush="#DCE3EE" BorderThickness="1" CornerRadius="8" Padding="8">
                            <StackPanel Orientation="Horizontal">
                                <ComboBox x:Name="MapLayerCombo" Width="150" Height="34" SelectedIndex="0" Margin="0,0,8,0">
                                    <ComboBoxItem Content="Street" />
                                    <ComboBoxItem Content="Satellite" />
                                    <ComboBoxItem Content="Terrain" />
                                    <ComboBoxItem Content="Altitude" />
                                </ComboBox>
                                <Button x:Name="CenterMapButton" Content="Center" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" />
                                <CheckBox x:Name="OverlayToggle" Content="Future overlay" IsChecked="True" VerticalAlignment="Center" />
                            </StackPanel>
                        </Border>
                        <Border x:Name="MapEmptyBadge" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,16,18" Width="320" Background="#F9FFFFFF" BorderBrush="#DCE3EE" BorderThickness="1" CornerRadius="8" Padding="12">
                            <StackPanel>
                                <TextBlock Text="Feeder overlay empty" Foreground="#132033" FontWeight="SemiBold" />
                                <TextBlock Text="Imported assets will be drawn over the selected basemap in a future build." Style="{StaticResource MutedTextStyle}" Margin="0,4,0,0" />
                            </StackPanel>
                        </Border>
                        <Border HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="16,0,0,18" Background="#F9FFFFFF" BorderBrush="#DCE3EE" BorderThickness="1" CornerRadius="8" Padding="10,7">
                            <TextBlock x:Name="MapReadout" Text="Lat -38.3350, Lng 175.1650, Zoom 10" Foreground="#39475D" FontWeight="SemiBold" FontSize="12" />
                        </Border>
                        <Border HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,14,14,0" Background="#F9FFFFFF" BorderBrush="#DCE3EE" BorderThickness="1" CornerRadius="8" Padding="10,7">
                            <TextBlock x:Name="MapAttribution" Text="Map tiles: OpenStreetMap / Esri / OpenTopoMap" Foreground="#667287" FontSize="11" />
                        </Border>
                    </Grid>

                    <Border Grid.Column="2" Background="#FFFFFF" BorderBrush="#DCE3EE" BorderThickness="1,0,0,0" Padding="16">
                        <StackPanel>
                            <TextBlock Text="Selected Asset" FontSize="16" FontWeight="SemiBold" Foreground="#132033" />
                            <Border Background="#F7F9FC" BorderBrush="#C8D3E3" BorderThickness="1" CornerRadius="8" Padding="18" Margin="0,14,0,0">
                                <StackPanel>
                                    <TextBlock Text="No asset selected" FontWeight="SemiBold" Foreground="#132033" />
                                    <TextBlock Text="Click map assets later. The basemap is connected now; feeder features and asset details will appear once processing exists." Style="{StaticResource MutedTextStyle}" Margin="0,8,0,0" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>
                </Grid>

                <ScrollViewer x:Name="ExplorerPage" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                    <StackPanel Margin="22"><TextBlock Text="Asset search" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" /><TextBlock Text="Model explorer" Style="{StaticResource PageHeadingStyle}" Margin="0,0,0,16" /><Border Style="{StaticResource PanelStyle}"><TextBlock Text="No searchable model yet. Once a graph model exists, this page can browse feeders, spans, transformers, devices and customer groupings." TextWrapping="Wrap" Foreground="#667287" /></Border></StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="ValidationPage" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                    <StackPanel Margin="22"><TextBlock Text="Quality gates" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" /><TextBlock Text="Validation" Style="{StaticResource PageHeadingStyle}" Margin="0,0,0,16" /><UniformGrid Columns="4"><Border Style="{StaticResource PanelStyle}"><StackPanel><TextBlock Text="Connectivity" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" /><TextBlock Text="-" FontSize="28" FontWeight="SemiBold" /></StackPanel></Border><Border Style="{StaticResource PanelStyle}"><StackPanel><TextBlock Text="Attribute completeness" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" /><TextBlock Text="-" FontSize="28" FontWeight="SemiBold" /></StackPanel></Border><Border Style="{StaticResource PanelStyle}"><StackPanel><TextBlock Text="Spatial consistency" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" /><TextBlock Text="-" FontSize="28" FontWeight="SemiBold" /></StackPanel></Border><Border Style="{StaticResource PanelStyle}"><StackPanel><TextBlock Text="Placement feasibility" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" /><TextBlock Text="-" FontSize="28" FontWeight="SemiBold" /></StackPanel></Border></UniformGrid><Border Style="{StaticResource PanelStyle}"><TextBlock Text="Validation rules are placeholders. Future routines will flag disconnected spans, missing device attributes, coordinate problems and inferred data." TextWrapping="Wrap" Foreground="#667287" /></Border></StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="AnalysisPage" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                    <StackPanel Margin="22"><TextBlock Text="Reliability studies" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" /><TextBlock Text="Analysis" Style="{StaticResource PageHeadingStyle}" Margin="0,0,0,16" /><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions><Border Grid.Column="0" Style="{StaticResource PanelStyle}"><StackPanel><TextBlock Text="Study setup" FontSize="16" FontWeight="SemiBold" /><TextBlock Text="Scenario: Baseline feeder model" Style="{StaticResource MutedTextStyle}" Margin="0,12,0,0" /><TextBlock Text="Objective: SAIDI / SAIFI reduction" Style="{StaticResource MutedTextStyle}" /></StackPanel></Border><Border Grid.Column="1" Style="{StaticResource PanelStyle}"><TextBlock Text="No analysis results. Reliability plots and optimisation comparisons will populate after algorithms are connected." TextWrapping="Wrap" Foreground="#667287" /></Border></Grid></StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="OutagesPage" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                    <StackPanel Margin="22"><TextBlock Text="Historical performance" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" /><TextBlock Text="Outages" Style="{StaticResource PageHeadingStyle}" Margin="0,0,0,16" /><Border Style="{StaticResource PanelStyle}"><TextBlock Text="No outage history imported. OMS extracts will eventually support baseline reliability calculations and feeder section comparisons." TextWrapping="Wrap" Foreground="#667287" /></Border></StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="ReportsPage" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                    <StackPanel Margin="22"><TextBlock Text="Outputs" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" /><TextBlock Text="Reports" Style="{StaticResource PageHeadingStyle}" Margin="0,0,0,16" /><Border Style="{StaticResource PanelStyle}"><DataGrid x:Name="ReportsGrid" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" IsReadOnly="True" MinHeight="220"><DataGrid.Columns><DataGridTextColumn Header="Report" Binding="{Binding Report}" Width="*" /><DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="160" /><DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="150" /></DataGrid.Columns></DataGrid></Border></StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="SettingsPage" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                    <StackPanel Margin="22">
                        <TextBlock Text="Workspace" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" />
                        <TextBlock Text="Settings" Style="{StaticResource PageHeadingStyle}" Margin="0,0,0,16" />
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Style="{StaticResource PanelStyle}">
                                <StackPanel>
                                    <TextBlock Text="Map defaults" FontSize="16" FontWeight="SemiBold" Foreground="#132033" />
                                    <TextBlock Text="Default center: The Lines Company network area" Style="{StaticResource MutedTextStyle}" Margin="0,12,0,0" />
                                    <TextBlock Text="Default basemap: Street" Style="{StaticResource MutedTextStyle}" />
                                </StackPanel>
                            </Border>
                            <Border Grid.Column="1" Style="{StaticResource PanelStyle}">
                                <StackPanel>
                                    <TextBlock Text="Prototype scope" FontSize="16" FontWeight="SemiBold" Foreground="#132033" />
                                    <TextBlock Text="Implemented: desktop navigation, native import staging, native export saves and live tile basemaps." Style="{StaticResource MutedTextStyle}" Margin="0,12,0,0" />
                                    <TextBlock Text="Pending: parsing, data normalisation, graph construction, validation rules and optimisation." Style="{StaticResource MutedTextStyle}" />
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#DCE3EE" BorderThickness="0,1,0,0">
                <TextBlock x:Name="StatusText" Text="Ready" VerticalAlignment="Center" Margin="22,0" Foreground="#667287" FontSize="12" />
            </Border>
        </Grid>
    </Grid>
</Window>
